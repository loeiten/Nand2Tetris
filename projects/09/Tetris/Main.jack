/** Initializes a new Tetris game and starts running it. */
class Main {
    function void main() {
        var TetrisGame game;
        let game = TetrisGame.new(0);
        do game.run();
        do game.dispose();
        return;
    }
}
